repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff check
        entry: uv run python -m ruff check src tests
        language: system
        pass_filenames: false

      - id: pytest
        name: pytest
        entry: uv run pytest -q
        language: system
        pass_filenames: false
